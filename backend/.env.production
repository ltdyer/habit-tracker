# this .env file is specifically meant to be copied into the backend container so 
# it can have access to any node process.env values
HOST = http://localhost
NODE_ENV = production
BACKEND_PORT = 3003